# ─── PDF gerados (manter só o .tex) ───
*.pdf
*.aux *.log *.toc *.lof *.lot *.out *.fls *.fdb_latexmk

# ─── C++ / CMake ───
edge/build/
edge/CMakeCache.txt
edge/CMakeFiles/
*.o *.a *.so

# ─── Rust ───
backend/target/

# ─── Node / Ionic / Angular ───
panel/node_modules/
panel/.angular/
panel/www/
panel/dist/

# ─── Variáveis de ambiente (nunca commitar segredos) ───
.env
.env.local
backend/.env

# ─── Modelos ONNX (binários grandes, usar Git LFS ou documentar download) ───
*.onnx

# ─── IDEs ───
.vscode/
.idea/
*.swp
